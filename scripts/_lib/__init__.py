# Shared library for scripts/ — extracted Sweep-4 (S18.18) to give the
# ontology guard, audit_api_contract, and runtime audit helpers a single
# parsing surface instead of duplicated regex + parse logic.
