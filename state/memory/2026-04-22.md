
## 2026-04-22 AM — Override-Chain Resolver + Validator

- [BOOTSTRAP-EVOLUTION] artifact=override_chain_resolver review_status=OK
  bootstrap=HAND-WRITTEN top_gap=render_function gap_state=LACKING
- [DELTA] `src/kg/override_chain_resolver.py`: classify opaque
  `override_chain_id` strings into {national, iso_admin, special_zone,
  unknown}; walk ancestor chain (zone → province → CN); validate a v4.3.1
  `override_chain_parents` list. Pure functions, no DB.
- [DELTA] `src/audit/data_quality_survey.py`: new `invalid_chain_count`
  defect — non-resolvable `override_chain_id` codes flagged. NULL is NOT
  counted (already tracked by `null_rate`). Distinguishes coverage gap
  from typo.
- [DELTA] `src/api/kg_api.py`: new endpoint
  `GET /api/v1/registry/resolve-override-chain?code=…` returns classification
  + label_zh + full ancestor chain. Used by UI to render breadcrumbs.
- [DELTA] `scripts/data_quality_survey.py`: render_summary gains
  `badchain=N` column.
- [TESTS] 10950 → 10989 passed (+39). 28 resolver unit tests (classify
  across 4 kinds, ancestor chain traversal, max_depth guard, validate
  chain id + parents list shape) + 6 survey tests (valid national/ISO/
  zone not flagged, unknown flagged, NULL not flagged here, mixed
  counting) + 4 REST tests (valid special-zone chain, national,
  unknown→valid:false, missing code rejected). Zero regressions.
- [CONTRACT] The override_chain_id column is now semantically typed at
  the application boundary. Cypher layer still accepts any string (no
  DB schema change), but survey + REST surface the semantic verdict.

## 2026-04-22 AM — Breadcrumb Renderer + Multiparent REST (crossed 11K)

- [BOOTSTRAP-EVOLUTION] artifact=override_chain_resolver review_status=OK
  bootstrap=HAND-WRITTEN top_gap=render_function gap_state=LACKING
- [DELTA] `override_chain_resolver.py`: adds `render_chain_for_operator()`
  (leaf-first or root-first, custom separator, unknown-code handling)
  and `resolve_multiparent()` which carries per-parent chain in a
  uniform shape even when individual entries are invalid.
- [DELTA] REST endpoint augmentations:
  - `GET /api/v1/registry/resolve-override-chain?code=…` now returns
    `breadcrumb_zh` (pre-rendered chain string)
  - new `GET /api/v1/registry/resolve-override-chain/multiparent?parents=…`
    (comma-separated; whitespace tolerant; rejects empty parents)
- [TESTS] 10989 → 11004 passed (+15, crossed 11K milestone). 6 renderer
  tests (breadcrumb contents, endswith root, root-first reversal, custom
  separator, unknown raw passthrough, empty safety) + 4 multiparent
  resolver tests (None/empty/all-valid/mixed-invalid) + 5 REST tests
  (breadcrumb in response, multiparent happy path, flagged invalid,
  whitespace trim, empty rejected). Zero regressions.
- [CONTRACT] Breadcrumb rendering is deterministic & pure: same code
  always produces same string. Safe to cache upstream. Multiparent
  shape mirrors single-parent shape (chain + valid + reason), so UI
  renders either with the same component.

## 2026-04-22 AM — M2 Inversion Closed: Code×Scope Co-Variance

- [BOOTSTRAP-EVOLUTION] artifact=jurisdiction_consistency review_status=OK
  bootstrap=HAND-WRITTEN top_gap=render_function gap_state=LACKING
- [DELTA] `src/kg/jurisdiction_consistency.py`: verifies
  (jurisdiction_code, jurisdiction_scope) joint validity. Four verdicts:
  `consistent` · `inconsistent` · `unknown_code` · `scope_not_set`.
  Decision table:
    national CN         → scope='national'
    iso_admin CN-XX     → scope in {subnational, municipal}
    special_zone        → scope = zone.scope (from registry)
    unknown             → unknown_code (separate from inconsistent)
- [DELTA] `data_quality_survey.survey_type` adds
  `inconsistent_scope_count` defect. NULL code/scope NOT counted here
  (coverage-gap concern); unknown_code NOT counted here (already caught
  by `invalid_chain_count`). Zeros in on the co-variance bug named by
  audit M2.
- [DELTA] REST: `GET /api/v1/registry/check-jurisdiction-consistency
  ?code=…&scope=…` returns verdict + reason + expected_scopes. Four
  verdicts disambiguate the fix (registry update vs operator correction
  vs fill scope).
- [DELTA] `scripts/data_quality_survey.py`: gains `badscope=N` column.
- [TESTS] 11004 → 11045 passed (+41). 27 consistency unit tests
  (national/iso_admin/special_zone/null/unknown/immutability/describe
  shape) + 7 survey tests (FTZ correct, FTZ wrong-scope, Hainan FTZ↔FTP
  ambiguity, NULLs not counted, unknowns not double-counted, compound)
  + 5 REST tests (pass, fail lists expected, unknown_code, scope_not_set,
  missing code rejected). Zero regressions.
- [CONTRACT] Audit M2 "jurisdiction-is-one-concept" finding is now
  mechanically enforceable. Rows with `CN-FTZ-SHA + municipal` are
  flagged at survey time AND at API time (for extraction pipelines
  that want fail-fast at write).

## [BOOTSTRAP-EVOLUTION] clause_inspector façade (M1 audit close-out, §8)

- [WHY] Six specialist modules (`argument_role_registry`,
  `argument_strength_renderer`, `override_chain_resolver`,
  `jurisdiction_consistency`, and two coverage auditors) each returned a
  different shape. Frontend and batch consumers had to learn all six
  error models and stitch them into one verdict. The audit flagged this
  as "integration surface complecting" — easy to get right once, easy to
  drift on every follow-up UI render.
- [DELTA] `src/kg/clause_inspector.py`: new `inspect(row) →
  InspectionResult` + `describe(result) → dict`. Pure function, no DB,
  no I/O. Accepts any dict with the documented keys; extra keys ignored
  so callers can pass a raw Kuzu row.
- [DELTA] Unknown-role branch fixed: `render_argument` raises on
  whitelist drift; inspector now catches the KeyError, renders strength
  alone, and surfaces `unknown_role` as a defect flag (the original
  comment claimed this already worked, but `render_role` never returned
  None for non-NULL unknowns — silent drift masked until now).
- [DELTA] REST: `POST /api/v1/inspect/clause` accepts the row JSON and
  returns the stable `describe()` shape. One round-trip replaces up to
  six.
- [TESTS] 11045 → 11080 passed (+35). 27 unit tests
  (clean/missing/prohibited/unknown_role/invalid_chain/multiparent/
  compound/describe-shape/frozen-invariant) + 8 REST tests
  (OpenAPI/clean/prohibited/inconsistent/multiparent/compound/
  extra-keys-ignored/empty-body). Zero regressions across the 11,080.
- [CONTRACT] Frontend now has a single contract to learn; drift between
  inspector output and specialist output becomes a detectable bug in
  exactly one file (`clause_inspector.py`), not a silent divergence
  scattered across six call sites.
- [GAP→PRESENT] `capability_catalog.render_function` (LACKING in
  post-edit-sota-audit.py catalog) partially advanced: `describe()` is
  the first pure render function that takes a typed aggregate and
  returns a JSON-stable dict. Flipping the catalog requires one more
  round of shape tests exercised via golden-file replay — tracked as
  top_gap for the next iteration.

## [BOOTSTRAP-EVOLUTION] golden-file replay harness (SQA-SF-d close-out)

- [WHY] The clause_inspector façade delivered today has a JSON-stable
  shape that UI + batch jobs depend on. Unit tests verify individual
  branches; they do NOT verify "the full rendered blob still looks like
  last week's blob." Silent shape drift (renamed key, dropped nested
  field, tuple-vs-list leak) is exactly the class of defect that
  passes green unit tests while breaking downstream.
- [DELTA] `tests/test_golden_replay.py`: generic harness; parametrized
  over fixture files; extends by adding one entry to `_REPLAYERS`.
- [DELTA] `scripts/regen_golden_fixtures.py`: canonical regeneration
  CLI with `--check` (dry-run → exit 1 on drift) and `--module` scope.
- [DELTA] `tests/golden/clause_inspector/*.json`: 8 committed fixtures
  (clean/prohibited/unknown_role/inconsistent/multiparent/compound/
  empty/special_zone_consistent). Together they cover every defect
  flag the inspector can emit.
- [TESTS] 11080 → 11091 passed (+11: 8 replay + 3 fixture hygiene).
  Zero regressions.
- [CAPABILITY] `render_function` capability partially flipped
  LACKING→PRESENT (pure-function JSON render exists and is pinned).
  `golden_file_replay` capability fully flipped LACKING→PRESENT.
  Flipping the AI-Fleet capability catalog dict is a separate edit
  outside this project; tracked as P1 in deliverable.md follow-ups.
- [CONTRACT] Schema drift now has a single fail signal: a red test
  whose diff shows exactly which key moved. Any future describe()
  contract change requires an explicit `regen_golden_fixtures.py` run
  in the same PR as the code — reviewer sees the before/after.

## [BOOTSTRAP-EVOLUTION] golden replay extension to 4 façades (queue-continue P1)

- [WHY] The harness delivered earlier today covered only `clause_inspector`.
  Three other façade paths (`jurisdiction_consistency.describe`,
  `override_chain_resolver.validate_chain_id`, `resolve_multiparent`)
  had identical drift-risk profiles — pure functions, JSON-stable
  shape, consumed by UI + REST — but no pinned fixtures.
- [DELTA] `tests/test_golden_replay.py`: `_REPLAYERS` dict grew 1→4.
- [DELTA] `scripts/regen_golden_fixtures.py`: 3 new scenario catalogs
  (jurisdiction_consistency×8, override_chain×6, multiparent×5).
- [DELTA] 19 new fixture JSONs under
  `tests/golden/{jurisdiction_consistency,override_chain,override_chain_multiparent}/`.
- [TESTS] 11091 → 11110 passed (+19: golden replays for the 3 new façades).
  Zero regressions. Replay file self-reports 30 tests; full suite ~66s.
- [CAPABILITY] `golden_file_replay` LACKING→PRESENT is now fully earned
  across every pure describe()/render() entry that UI touches. The one
  LACKING capability remaining in the pattern is `render_function`
  composition (golden-test a render function that's the composition of
  two specialists) — deferred as it requires building one first.
- [PATTERN] Adding a new façade to the harness is a 3-line PR:
    1. New `_replay_xxx(row) -> dict` in `test_golden_replay.py`
    2. Entry in `_REPLAYERS`
    3. Scenario list + `MODULES` entry in `regen_golden_fixtures.py`
  followed by `regen_golden_fixtures.py` to seed fixtures.

## [BOOTSTRAP-EVOLUTION] operator CLI for clause_inspector (SQA-SF-e close-out)

- [WHY] The clause_inspector module docstring promised three consumers:
  REST (shipped), UI batch render loops (shipped as describe()), and
  `scripts/inspect_clause.py` (the operator CLI). The CLI was the one
  gap that kept on-call runbooks pinned to "curl the REST endpoint" —
  fine when the service is up, not fine during an incident where the
  API might be the thing being debugged.
- [DELTA] `scripts/inspect_clause.py`: argparse CLI; reads row JSON
  from `--row` or stdin; emits human / JSON / quiet modes; exit codes
  0/1/2 carry the verdict (CI-probe friendly).
- [DELTA] `tests/test_inspect_clause_cli.py`: 10 tests covering exit
  codes (clean / defect / invalid JSON / non-dict / empty stdin),
  output shapes (human summary / JSON / quiet), stdin path.
- [TESTS] 11110 → 11120 passed (+10). Zero regressions. Full suite ~83s.
- [CONTRACT] All three promised consumers now exist. Module docstring
  is no longer aspirational; drift between docstring and reality is
  zero at this moment. Any future docstring edit that adds a consumer
  must land alongside that consumer or fail review.
- [RUNBOOK] On-call can now `echo '<row>' | .venv/bin/python scripts/
  inspect_clause.py --json` and pipe the result into jq, grep, or a
  Sentry annotation without any service dependency.

## [BOOTSTRAP-EVOLUTION] batch inspect endpoint (SQA-SF-f close-out)

- [WHY] The clause_inspector docstring promises "UI batch render loops
  (replace 6 API calls with 1)". Shipped was the 6→1 on role/strength/
  chain/multiparent/consistency/prohibition axes, but the ROW→ROW axis
  was still 1:1 — a 1000-row clause page meant 1000 HTTP round-trips.
  The collapse is right only if both axes collapse.
- [DELTA] `POST /api/v1/inspect/clause/batch` — envelope
  `{"rows": [...]}` → `{"count", "clean_count", "defect_count",
  "results": [...]}`. Hard cap 1000 rows (in-code constant, not knob:
  the inspector is sub-ms, request size / client memory is the real
  bound).
- [DELTA] `ClauseInspectBatchRequest` pydantic model reuses
  `ClauseInspectRequest` per-row with `extra="ignore"` — same tolerance
  for raw Kuzu rows.
- [TESTS] 11120 → 11127 passed (+7 batch: OpenAPI / empty batch /
  mixed clean+defect / input-order preservation / 1001-row rejection /
  extra-keys tolerance / single-vs-batch parity).
- [CONTRACT-PARITY] `test_batch_shape_matches_single_for_same_row` pins
  the invariant "per-row shape in batch == single endpoint shape". Any
  future drift between single and batch ships a red test.
- [CONSUMER-VALUE] UI already wired to `/clause` needs to change only
  its loop, not its response parser. Migration delta = 3 lines.
- [ROLLUP] `clean_count` / `defect_count` let the UI render a summary
  badge at page top without re-iterating the full results array.

## [BOOTSTRAP-EVOLUTION] survey → clause_inspector delegation (SQA-SF-g)

- [WHY] Hickey's complecting, caught late: the data_quality_survey
  module had its OWN copies of "what counts as a prohibited role / bad
  chain / inconsistent scope" — exactly the three axes the
  clause_inspector façade had just consolidated. Silent divergence
  between the two was only a matter of time: someone adds a flag to
  the inspector, the survey keeps reporting old numbers, the status
  board shows green while prod is red.
- [DELTA] `src/audit/data_quality_survey.py`:
    - Deleted `_count_prohibited_roles`, `_count_invalid_override_chains`,
      `_count_inconsistent_scope` (three separate loops, three
      specialist imports).
    - Added `_clause_defect_counts` (one loop, one import:
      `clause_inspector.inspect`).
    - `survey_type()` now calls the helper once, unpacks the dict.
- [DELTA] Imports cleaned: removed `is_prohibited_in_tax_law`,
  `_check_consistency`, `is_valid_code` — now reachable through the
  inspector, no direct specialist calls left in this module.
- [TESTS] 11127 → 11127 passed. Test count UNCHANGED. That is the
  point: the 45 existing survey tests all still pass with no code
  changes, proving the refactor is behaviour-preserving. If any
  counter had drifted by even one row, the existing fixtures would
  have tripped.
- [INVARIANT] Future additions to clause-axis defect semantics land in
  exactly one file (`clause_inspector.inspect`). The survey picks them
  up on next run without touching this module. The single-source-of-
  truth rule is now enforced by code, not by code review.
- [BOUNDARY] `_count_jurisdiction_mismatches` stayed put — it handles
  the NULL-XOR defect (code set without scope or vice versa), which is
  a data-coverage axis the inspector is not on the hook for. Deleting
  it would have conflated two different axes.

## [SWARM] ontology-audit-swarm: 2026-04-22-session-delta-audit

- outputs/reports/ontology-audit-swarm/2026-04-22-session-delta-audit.md (EN canonical)
- outputs/reports/ontology-audit-swarm/2026-04-22-session-delta-audit.html (ZH, BCG green)
- 方法论：Inversion · First Principles · Polanyi 三棱
- 11 条批评 → 3 Must-Fix / 5 Should-Fix / 3 Could-Fix / 3 Won't-Fix
- 伪 SOTA 观察清单：测试数、sub-ms 纯函数、单一事实源、capability catalog 翻转、1000 硬上限、"六个"模块计数
- 今日真实 SOTA 增量：SF-c 门面 / SF-d golden / SF-f 批量 / SF-g 委托
- 下一轮 top_gap：S-b（demo graph 跑 survey 形成首个真实 defect-rate 基线）

## [BOOTSTRAP-EVOLUTION] operator Web UI (SQA-SF-h close-out)

- [WHY] The session-delta audit earlier today flagged "eat-your-own-
  dogfood gap — nothing today ran in a real browser against a real
  clause row." CLI + REST + golden tests all prove the structure, but
  none prove the UX. A façade that nobody uses from a browser is an
  IDL, not a product.
- [DELTA] `src/web/inspect.html` (~400 lines vanilla HTML + JS):
  8pt spacing tokens, grayscale scale 0-9, semantic ok/warn/err
  palette, single Primary Action ("审核"), two-tab single/batch mode,
  inline spinner for loading, role=alert live region for errors,
  skip-link, responsive grid collapsing to single-column ≤860 px.
- [DELTA] `GET /inspect` + `GET /inspect.html` in `src/api/kg_api.py`.
- [DELTA] `tests/test_inspect_web_smoke.py` (8 tests): route wiring,
  a11y-string presence, no-secret-leak, endpoint-string match, three
  real-request smoke tests that mirror what the UI sends.
- [VALIDATION via chrome-devtools MCP]
    - Golden path: clean row → CLEAN verdict; defect row → 2 flags
      correctly translated (税收法定禁止 / 辖区代码与作用域不一致)
    - Batch path: 3 rows → 2 clean, 1 defect, per-row chips
    - Failure path: `{this is not json}` → yellow alert card naming
      line 1 with parse error
    - Console: 0 error, 0 warning (favicon silenced with inline SVG)
    - Network: only fonts + POST inspect endpoints; no X-API-Key /
      Authorization leaked
    - Responsive: 375×720 single-column screenshot captured
    - Keyboard: skip-link works, tab ArrowRight switches modes
- [SCREENSHOTS] 5 files under outputs/reports/ontology-audit-swarm/
  screens/ — initial / defect / batch / error / mobile
- [DOCS] USER_EXPERIENCE_MAP.md v0.3: Journey D + failure paths +
  quality gates + screenshot evidence paths
- [TESTS] 11127 → 11135 passed (+8). Zero regressions.
- [GAP CLOSED] Audit item "nothing ran in a real browser today" no
  longer true. The façade is dogfoodable and dogfooded.

## 2026-04-22 · SQA-SF-i — Real product integration (supersedes SF-h scope)

- [BOOTSTRAP-EVOLUTION] artifact=web/src/app/components/ClauseInspector.tsx
  review_status=OK bootstrap=HAND-WRITTEN
  top_gap=cross_format_build gap_state=PRESENT-BY-REUSE
  (reused cognebula-theme tokens; no new palette introduced)
- [BOOTSTRAP-EVOLUTION] artifact=web/src/app/lib/kg-api.ts
  review_status=OK bootstrap=HAND-WRITTEN
  top_gap=golden_file_replay gap_state=LACKING
  (no Playwright test yet; TS types match backend describe() field-for-field
  verified via curl round-trip)
- [BOOTSTRAP-EVOLUTION] artifact=web/src/app/expert/data-quality/page.tsx
  review_status=OK bootstrap=HAND-WRITTEN
  top_gap=render_function gap_state=LACKING
  (still no render() separated from fetch() — same gap as rest of the web app)
- [SWARM] ontology-audit-swarm: session-delta-audit + real-frontend-integration
  → web/src/app/components/ClauseInspector.tsx + data-quality/page.tsx
  | decision=Option B (data-quality section, not standalone page)
  | audit-path=outputs/reports/ontology-audit-swarm/2026-04-22-session-delta-audit.{md,html}

## Scope error caught & fixed (self-critique)

- **Failure**: SF-h delivery claimed "operator Web UI" but built a standalone
  FastAPI-served HTML page at `/inspect`. User called it out by showing the
  real product UI screenshot at `ops.hegui.org/expert/kg/`.
- **5-Why root cause**: treated `src/web/` as "the frontend" without reading
  `/web/` (Next.js app) at the project root. Glob + ls at project root would
  have surfaced the Next.js app immediately.
- **Fix**: SF-h downgraded to "dev-side API self-test page". SF-i added as
  the real product integration.
- **Rule confirmed** (from memory `feedback_read_before_mapping.md`): read
  target structure before committing to a scope/mapping. Name collision
  (`src/web/` vs `web/`) ≠ capability equivalence.

## 2026-04-22 · DEPLOY — SF-i reaches production

- [DEPLOY] target=ops.hegui.org (internal) + app.hegui.org (shared static)
  source=main @ 5bf7dff · build-id=`web/out/` (next 16.2.1 static export)
  path=rsync -az --delete web/out/ contabo:/home/kg/cognebula-web/
  chown+chmod=www-data:www-data o+rX
  chunk-hash=14eh-z670e0-1.js
- [VERIFY-EXTERNAL] https://app.hegui.org/_next/static/chunks/14eh-z670e0-1.js
  HTTP 200 · 30456B · grep '条款语义审核' = 1 match
  https://app.hegui.org/api/v1/health HTTP 200
- [VERIFY-PENDING] /expert/data-quality/ HTML body behind basic auth on
  ops.hegui.org — user-side manual verification required with credentials
- [BOOTSTRAP-EVOLUTION] artifact=web/out/ (full next export)
  review_status=OK bootstrap=PIPELINE-PRODUCED
  top_gap=rollback_manifest gap_state=LACKING
  (no prior-build tarball kept on VPS for 1-command rollback; rollback
   path = git checkout <prev SHA> && rebuild && rsync, ~3 min)

## 2026-04-22 · GAP-CLOSURE — rollback_manifest flipped LACKING → PRESENT

- [BOOTSTRAP-EVOLUTION] artifact=scripts/deploy_web.sh
  review_status=OK bootstrap=HAND-WRITTEN
  top_gap=rollback_manifest gap_state=PRESENT
  - Snapshot-before-rsync: tar -czf cognebula-web-snapshot-<ts>-<tag>.tgz
    runs on VPS before any destructive rsync. First snapshot
    cognebula-web-snapshot-20260422T142532Z-sf-i.tgz (1.2MB) captures the
    just-deployed SF-i state.
  - Post-rsync content-signal verification: greps '条款语义审核' in
    deployed expert/data-quality/index.html. Fails loud if missing.
  - Rollback subcommand: scripts/deploy_web.sh rollback extracts the
    newest snapshot, atomically swaps cognebula-web/ with the previous
    tree, reapplies www-data ownership. Old tree kept at
    cognebula-web.rollback until next deploy (safety net for
    rollback-of-rollback).
  - Verification: bash -n clean; VPS ssh+ls confirms snapshot present.
    Smoke-deploy not run — deliberately keeping a separation between
    "write tool" and "exercise tool under a new SF-j delivery".

## Schedule for next session

- [SCHEDULE] SF-j — KG node-detail "审核此条" deep-link (swarm Option C)
  requires reading web/src/app/expert/kg/page.tsx (45KB) first; not
  suitable for blind autonomous start.
- [SCHEDULE] MF-4 — run survey against data/finance-tax-graph.demo to
  produce first real defect-rate baseline. Needs demo graph fixture
  verified present on local disk.
- [SCHEDULE] Release runbook (MF-2 from earlier delta-audit). With
  deploy_web.sh + snapshot/rollback in place, the runbook is now mostly
  "call the script"; remaining content is pre-deploy checklist +
  post-deploy smoke script + contact-on-failure tree.
