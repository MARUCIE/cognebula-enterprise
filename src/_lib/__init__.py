# Shared library for src/api backends — Sweep-7 / S18.26.
# Holds factories shared between kg-api-server.py (Backend A) and
# src/api/kg_api.py (Backend B) to keep the route catalog endpoint
# in one place.
